projection;
strict ( 2 );
use draft;

define behavior for ZC_DOC_ATT //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}